apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mcp-oauth2-proxy.fullname" . }}
  namespace: {{ include "mcp-oauth2-proxy.namespace" . }}
  labels:
    {{- include "mcp-oauth2-proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: proxy
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
